/**
 * Entry único de JS da página inicial (vanilla, sem dependências).
 * Cada módulo tem guarda própria e só roda se o elemento existir.
 * Sticky CTA e banner de cookies ficam nos próprios componentes.
 */
import './utm';
import './gallery';
import './countdown';

export {};
